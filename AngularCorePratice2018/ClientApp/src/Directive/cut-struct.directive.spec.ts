import { CutStructDirective } from './cut-struct.directive';

describe('CutStructDirective', () => {
  it('should create an instance', () => {
    const directive = new CutStructDirective();
    expect(directive).toBeTruthy();
  });
});

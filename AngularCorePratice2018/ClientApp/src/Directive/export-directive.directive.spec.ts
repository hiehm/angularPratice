import { ExportDirectiveDirective } from './export-directive.directive';

describe('ExportDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ExportDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
